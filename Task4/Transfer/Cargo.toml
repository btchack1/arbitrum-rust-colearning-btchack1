[package]
name = "Transfer"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
dotenvy = "0.15"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
ethers = { version = "2", features = ["rustls"] }
